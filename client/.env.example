# Copy to .env.local for local overrides, or set in Netlify's UI for production.
# URL of the deployed Socket.io server (no trailing slash).
# Local dev can leave this unset — it defaults to http://localhost:3001.
VITE_SOCKET_URL=https://mahjong-server.onrender.com
